<view class="page">
  <view class="page__bd page__bd_spacing">
    <view class="flex-wrp flex-row">
      <view class="flex-item series" wx:key wx:for="{{array}}" wx:for-item="item">
        <view class="poster">
        </view>
        <view class="title">
          <view class="name">{{item.name}}</view>
          <view class="chapter">{{item.chapter}}</view>
        </view>
        <view class="tips">
        </view>
      </view>
    </view>
  </view>
</view>