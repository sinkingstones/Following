<view class="page">
  <view class="page__bd page__bd_spacing">
    <view class="flex-wrp flex-row">
      <view class="flex-item" wx:key wx:for="{{array}}" wx:for-item="item">
        <view class="series">
          <text class="">{{item.name}}</text>

          <!--<text class="after">{{item.after}} 天</text>-->

          <!--<text class="chapter">{{item.chapter}}</text>-->
        </view>
      </view>
    </view>
  </view>
</view>